<form method="post" action="{{ path('posts_delete', {'idp': post.idp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.idp) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'basef.html.twig' %}

{% block b %}
       {% block Blogs %}
           {% for p in posts %}
           <div class="row pt-md-3">
           <div class="col-lg-9 col-md-10 mx-auto">
           <div class="accordion" id="accordionExample">
           <div class="subject-card mt-4">
               <div class="subject-card-header p-0" id="headingOne">
                   <a href="#" class="card__title p-3 p-lg-4 collapsed d-block" data-toggle="collapse" data-target="#collapseOne" aria-expanded="false"
                      aria-controls="collapseOne">
                       <div class="row">
                           <div class="col-md-5 subject-img">
                               <img src="{{p.imgpost }}"style="height:10cm;width:8cm;" alt="">
                           </div>
                           <div class="col-md-7 subject-content mt-md-0 mt-4">
                               <h3>{{ p.iduser }}</h3>
                               <p><a href="{{ path('posts_show1', {'idp': p.idp}) }}">{{ p.contenup }}</a>
                               <span class="course-number"><span class="fa fa-comments-o" aria-hidden="true"></span> {{ p.nbcmt }}</span>
                               <p {{ p.nbcmt }} </p>
                               <span class="course-number"><span class="fa fa-heart" aria-hidden="true"></span> {{ p.likes }}</span>
                               <p {{ p.likes }} </p>
                               <span class="course-number"><span class="fa fa-eye" aria-hidden="true"></span> {{ p.views }}</span>
                               <p {{ p.views }} </p>
                               <span class="course-number"><span class="fa fa-comments-o" aria-hidden="true"></span> {{ p.report }}</span>
                               <p {{ p.report }} </p>
                               <span class="date"aria-hidden="true">
								<label  rel="bookmark" class="entry-date">
									{{ p.dateP | date }}
								</label>
							</span>
                               <td>
                                   <a  class="btn btn-primary " href="{{ path('posts_edit', {'idp': p.idp})}}">Edit post</a>
                                   <a  class="btn btn-primary " href="{{ path('comments_new', {'idp': p.idp}) }}">Add Comment</a>
                               </td>
                           </div>
                              </div>
                              {#  <div class="checkbox checkbox-secondary">

 <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit() href="{{ path('posts_indexf') }}">Like</button>
                               #</div>
                               <div class="checkbox checkbox-secondary">

                                   <button type="SubmitType" class="btn btn-primary " onClick="validateSubmit() href="{{ path('posts_indexf') }}">Report</button>
                               </div>#}

                       </div>
               </div>
           </div>
                   </div>
               </div>
           </div>
           {% endfor %}

       {% endblock %}


                               <div class="w3l-index1">
                                   {% block Blogsup %}
                                   <div class="content">
                                       <div class="container">
                                           <div class="row align-items-center">
                                               <div class="col-lg-6 content-left">
                                                   <h3>Alpha Fitness is the new Alpha
                                                   </h3>
                                                   <p class="mt-3 mb-lg-5 mb-4">Start one of our 1000 high quality sports from the world’s leading events today!</p>

                                                       <a href="{{ path('posts_new')}}" class="btn btn-primary theme-button mb-2">Add post</a>


                                               </div>
                                           </div>
                                           <div class="clear"></div>
                                       </div>
                                   </div>

                               </div>
                               {% endblock %}
    {#    <div class="=vote">
            <div class="vote_bar">
                <div class="vote_progress"></div>
            </div>
           <div class="vote_btns">
               <div class="vote_btn vote_like">145</div>
               <div class="vote_btn vote_dislike">2</div>

           </div>

        </div>
                <div>Like/Dislike</div>#}
{% endblock %}
